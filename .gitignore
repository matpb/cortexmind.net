# Local dev secrets for `wrangler pages dev` — never committed.
.dev.vars

# wrangler cache/state
.wrangler/

# local tooling noise
.dev-server.pid
.dev-server.log

# SvelteKit build
node_modules/
build/
.svelte-kit/
src/lib/paraglide/
e2e-out/
